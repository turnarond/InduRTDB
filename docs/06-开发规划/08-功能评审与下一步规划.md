# InduRTDB 功能评审与下一步规划

**版本：1.0（草稿，待评审）**
**日期：2026-09-23**
**基线：v3.1.0**
**状态：决策已确认（D1=A / D2=C / D3=B），v3.2 任务待实施**

> 本文回答三个问题：① 当前到底有哪些功能（以代码为准）；② 对照优秀开源工业实时点位数据库，缺什么、差在哪；③ 下一步怎么规划、每个版本的边界在哪。
> 本文是**评审结论**，不是实现计划。每个落地任务仍需另建分支 + 实现计划（遵循 TDD 与评审约定）。

---

## 1. 评审方法与范围

- **代码基线**：`v3.1.0`，以 `src/`、`include/indurtdb/indurtdb.h`、`tests/` 为事实来源。
- **文档基线**：`docs/01-白皮书/` 三份白皮书、`docs/06-开发规划/05-技术演进规划.md`、`README.md`、`CHANGELOG.md`。
- **对标对象**：Eclipse iceoryx/iceoryx2（零拷贝共享内存数据面）、Apache IoTDB / TDengine / Machbase Neo（工业时序库）、openHistorian / PI System（实时历史库）、Redis（内存数据中枢）、OPC UA 信息模型 IEC 62541-8（工业语义）。
- **原则**：只对标**同层能力**（共享内存实时层），不把时序库的历史/压缩能力算作 InduRTDB 的必补项——它已在演进规划中明确划归 node-server。

---

## 2. 当前功能盘点（以代码为准）

v3.1.0 的本质是：**单进程单例 + 全局单 Seqlock 的共享内存点位表**。

| 分类 | 能力 | 实现要点 |
|---|---|---|
| 存储 | 共享内存布局 | `/indurtdb_<instance_id>`；`Header 64B + N×128B 点位 + M×16B 订阅心跳`；`IRT_STATIC_ASSERT` 锁定，与 v2.x 逐字节兼容 |
| | 数据类型 | bool / int32 / double / string[31+1] |
| | 工业语义 | quality（GOOD/BAD/TIMEOUT/SUBSTITUTED）+ unit + access + name[64] + timestamp_ns |
| | 元数据加载 | `load_config`：key=value（初始化）+ 自研 YAML 点位元数据解析 |
| API | 26 个 C 函数 | 单点 4 类型读写、`read_point`、`peek`（**单拷贝**到 `_Thread_local`）、`read_range`、`write_range_×3`、订阅、统计 |
| 订阅 | 进程内回调 | 256 定长槽位；共享内存侧仅存心跳表（pid + last_heartbeat_ns）+ zombie 清理 |
| 可靠性 | 崩溃自愈 | owner 崩溃接管（`kill(pid,0)` 存活检查 + `irt_shm_os_claim_ownership`）、奇数 write_seq 恢复、fork 安全（`owner_pid != getpid()` 清除 owner 标记） |
| 运维 | 超时检测 | `check_timeouts(ns)` 全量扫描，超时点标 QUALITY_TIMEOUT（写冲突时跳过，best-effort） |
| 平台 | 双 OSAL | POSIX（已验证）+ SylixOS（代码存在，未验证） |
| 集成 | 三消费路径 | find_package / pkg-config / FetchContent；子项目隔离；starter 模板；`verify_consume.sh` |
| 质量 | 测试与 CI | gtest **26 套件 / 126 用例**（ctest 注册 **16 个**：15 单元 + 1 多进程集成），全绿；soak / 故障注入 / bench 脚本；GitHub Actions Debug+Release 双配置；`-Wall -Wextra -Werror` 零警告 |

**已实测（x86, Release -O2）**：write_int32 P99 0.386μs、read_int32 P99 0.068μs、peek P99 0.062μs，均优于 P99 ≤10μs 设计目标。ARM 未实测。

---

## 3. 白皮书评审：定位漂移（一等缺陷）

### 3.1 三份白皮书 + 演进规划互相冲突

| 文档 | 定位 | 对外接口 | 集群 |
|---|---|---|---|
| 白皮书 1.0（对应 v2.1.0） | 取代 Modbus 的安全数据底座 | OPC UA Bridge（v3.2 路线图） | 未提 |
| 白皮书 2.0（v3.1.0） | PLC 实时数据中枢，对标 Redis | VSOA RPC + `/cluster/sync` | 📋 未实现，但在 §1/§9 当作卖点陈述 |
| 用户体验白皮书 3.0 | 工业控制系统"神经系统" | 未提 | ⏳ 后续 |
| **技术演进规划（最新，2026-08-10）** | **node-server 的底层共享内存实时数据层** | **复用 node-server 的 OPC UA / SQLite，不重复造轮子** | **明确不做** |

> **结论**：白皮书 1.0 / 2.0 的定位、对外接口、集群口径，与最新架构决策**直接冲突**。按"文档是唯一对外接口、不得腐败"的约定，修订白皮书是 P0 任务，且优先级高于任何新功能。

### 3.2 文档与实现不符逐条清单

| # | 文档表述 | 实现事实 | 出处 |
|---|---|---|---|
| 1 | 2.0：`peek` 为"零拷贝，返回共享内存指针" | 实为**单拷贝**到 `_Thread_local`，同线程下次 peek 覆盖（README/头文件已更正，白皮书未同步） | `src/core/irt_point_manager.c`、`indurtdb.h:79-82` |
| 2 | 2.0："分片：instance_id 多实例隔离 ✅" | **单进程单例**，不能同时开两个实例；测试注释明写"当前单例架构不允许同时双开" | `tests/unit/test_c_multi_instance.cpp:26` |
| 3 | 1.0：OSAL 抽象 `ISharedMemory`/`ITime`/`IThreading`/`INotification` | 只有 time + shm 两类函数，**无线程/通知实现** | `src/osal/irt_osal.h` |
| 4 | 1.0：24 个函数、v2.1.0 | 实际 26 个函数、v3.1.0 | `indurtdb.h` |
| 5 | 1.0 §7："订阅推送延迟 ≤50μs（通知到订阅进程）" | **跨进程订阅不存在**，该指标无法测量 | `src/core/irt_subscription.c` |
| 6 | 2.0：§1 称"集群就绪" | §2.1 才标 📋 未实现；演进规划明确"不做" | 同上 |
| 7 | 2.0 §7 安全对比表把"VSOA Password/加密"算作 InduRTDB 能力 | 库本身**零鉴权、零加密**，仅静态 `access` 字段 | `indurtdb.h:37-38` |
| 8 | 1.0 §5.1："写入吞吐 ≥50k 点/秒" | x86 实测 5.2M op/s，指标陈旧且偏保守 | `tests/bench/` |
| 9 | **三份白皮书均未声明** | **多写者并发冲突时 `write` 直接返回 `-2 busy`，不重试、不阻塞** | `src/core/irt_point_manager.c:37` |
| 10 | 测试数量口径混乱（曾分别写 14 / 15 / 26） | 实为**两个粒度**：gtest **26 套件 / 126 用例**；ctest 注册 **16 个**（15 单元 + 1 多进程集成）。此前被误判为互相矛盾 | `ctest -N`（16）+ `grep -c "^TEST("`（126） |
| 11 | `04-技术文档/01-Seqlock算法设计文档.md` 仍描述 `irt_seqlock_read()` | 该函数因返回裸指针存在 TOCTOU 脏读**已移除**；现行 `peek` 为单拷贝到 `_Thread_local` | `src/internal/irt_seqlock.h:40` |

---

## 4. 竞品对标

### 4.1 对标矩阵

| 对标对象 | 定位 | 关键能力 | InduRTDB 差距 |
|---|---|---|---|
| **Eclipse iceoryx / iceoryx2** | 真零拷贝共享内存数据面（汽车/任务关键） | 跨进程 pub/sub、RouDi 守护进程统一管生命周期与资源、服务发现、请求-响应 | **缺跨进程通知、缺服务发现、缺生命周期守护** |
| **Apache IoTDB / TDengine / Machbase Neo** | 工业时序库，端边云协同 | 高压缩列存、SQL、订阅、OPC UA/MQTT 原生接入 | 历史/压缩已划归 node-server（✅ 决策正确）；但**缺变化上报 / deadband** 这一上送侧能力 |
| **openHistorian / PI System** | 实时历史库 | 内存快照 + ACID 归档 + 点位元数据（量程/死区/压缩）+ **按名查找** + 历史查询 | **缺按名查找、缺量程、缺死区**；归档不在边界内 |
| **Redis** | 内存数据中枢 | KV、Pub/Sub、TTL、持久化、ACL | 对等项基本齐；**缺字符串键、缺跨进程 Pub/Sub** |
| **OPC UA 信息模型（IEC 62541-8）** | 工业语义事实标准 | `DataValue = Value + StatusCode + SourceTimestamp + ServerTimestamp`；AnalogItem 含 EURange / EngineeringUnits；Deadband 与 DataChangeFilter；StatusCode limit bits（Low/High/Constant） | quality 仅 4 值、**只有 ServerTimestamp**、无量程、无死区 |

### 4.2 对标结论

InduRTDB 的**长板**（ns~μs 级共享内存访问、零依赖静态库、崩溃自愈、布局字节兼容）在当前赛道上确实是差异化优势，iceoryx 面向大 payload 数据面、TSDB 面向历史分析、Redis 面向网络 KV，**没有直接竞品同时覆盖"嵌入式 .a + 工业语义 + 共享内存"**。

InduRTDB 的**短板**集中在两处，且都不是性能问题：
1. **跨进程协同能力**：只有数据共享，没有变更通知与发现——进程间只能退化为轮询，这恰恰抵消了共享内存带来的延迟优势。
2. **工业语义深度**：quality 只有 4 个枚举、时间戳只有服务端时间、没有量程与死区，接入 OPC UA / SCADA 时语义会被降级或丢失。

---

## 5. 差距清单与优先级

| # | 差距 | 影响 | 优先级 | 归属版本 |
|---|---|---|---|---|
| G8 | **跨进程变更通知缺失**（订阅仅进程内） | HMI/控制进程只能轮询，延迟优势被抵消 | **P0** | v3.3 |
| G9 | **多写者写冲突返回 busy 不重试** | 多进程并发写不可靠，语义不确定（未文档化） | **P0** | v3.3 |
| G10 | **无字符串键 / 按名查找** | 工程配置用点位名，集成方必须自建映射层 | **P0** | v4.0（shm 内索引，决策 D3） |
| G11 | **白皮书与实现不符 9 条** | 文档腐败，对外接口失真，选型误导 | **P0** | **v3.2** |
| G4 | SylixOS 未验证（既有缺口） | 国产 RTOS 目标市场空白 | P0（若含该市场） | v3.3 |
| G12 | 数据类型不全（缺 int64/uint32/float/blob；string 仅 31 字符） | 部分现场点位无法表达 | P1 | v3.3 |
| G13 | 语义不全（SourceTimestamp、StatusCode limit 位、EURange、deadband/RBE） | 接入 OPC UA/SCADA 时语义降级 | P1 | v3.3 |
| G14 | 单进程单例 | 一个进程无法管多个 RTDB 实例 | P1 | v4.0（ABI 变更） |
| G15 | 容量固定不可扩容 | 点位规模需停机调整 | P1 | v4.0（ABI 变更） |
| G16 | 无 CRC/校验、无 shm 版本协商与迁移策略 | 静默损坏无法发现；版本不匹配行为未定义 | P1 | v3.3 |
| G17 | 无诊断可观测（fd/shm 泄漏检测、per-point 统计、巡检工具） | 7×24 长稳问题难定位 | P1 | v3.3 |
| G18 | 语言绑定（宣称 FFI 可用，无实际产物） | 生态落地成本高 | P2 | v3.3+ |
| G19 | 工具集缺失（Python 系列工具：dump/巡检/灌数据/冒烟） | 测试、部署、验证靠手工 | P2 | v3.3 |
| G20 | 安全加固（进程级鉴权、审计） | 应归 node-server / Bridge，不在库边界 | P2 | 不在本库 |

---

## 6. 决策结论（已确认）

| 编号 | 决策点 | 结论 | 影响 |
|---|---|---|---|
| **D1** | 产品定位 | **A. node-server 底层数据层**。对外只讲"共享内存实时数据层"；北向 OPC UA、持久化、集群一律归 node-server / Bridge，不重复造轮子。白皮书 1.0 / 2.0 移入归档，重制统一版白皮书 4.0 | 定位口径唯一；白皮书 2.0 中的 VSOA RPC、`/cluster/sync`、Redis 分片对标等表述全部下线 |
| **D2** | v3.2 范围 | **C. 只做文档治理**。修 9 条文档与实现不符 + 白皮书重制；能力类任务全部下放 v3.3 | v3.2 为**纯文档版本**，不改代码、不动 ABI |
| **D3** | 按名查找 | **B. shm 内 name→id 索引**（全局一致、支持点位发现），属 ABI 变更 → **v4.0** | 需重构评审 + shm 布局 v2 + 迁移工具；v3.2/v3.3 维持数字 ID |

---

## 7. 下一步规划（版本边界）

### v3.2 ——「文档治理」版本

**边界：只改文档，不改代码、不改 ABI、不做新功能。** 版本性质为对外口径校正，仍打 tag 发布。

> **实施状态（2026-09-23）**：T1–T6 已全部实施于分支 `feature/v3.2-文档治理`，**待评审后合并**；库版本号保持 3.1.0 不变，建议 tag `v3.2.0-docs`。

| # | 任务 | 内容 | 验收标准 |
|---|---|---|---|
| T1 | 白皮书 4.0 重制 | 出统一版白皮书，定位 = "node-server 底层共享内存实时数据层"；性能用 x86 实测值；明确不做什么 | 定位/边界/性能/不做清单四节与代码一致；评审通过 |
| T2 | 旧白皮书归档 | `01-白皮书/01-白皮书1.0.md`、`02-白皮书2.0.md` 移入 `01-白皮书/归档/`，文件头标注"历史版本，口径已废弃，以白皮书 4.0 为准" | 归档完成且目录编号连续 |
| T3 | 用户体验白皮书对齐 | 修订 `03-用户体验白皮书.md`（可重编号为 `02-`），对齐新定位与 peek 单拷贝语义 | 与白皮书 4.0 口径一致 |
| T4 | 修 11 条文档与实现不符（实施中发现比原评估多 2 条） | 见 §3.2 表格：peek 单拷贝、单例限制、OSAL 实际接口、函数数量/版本、跨进程订阅不存在、集群未实现、鉴权/加密不在库内、吞吐指标陈旧、多写者 busy 不重试、测试数量、已移除的 `irt_seqlock_read()` | 逐条销项，每条给出"改后表述 + 代码出处" |
| T5 | 行为约束补写 | `05-SDK手册` 补：peek 语义与失效条件、单进程单例限制、写冲突返回 `-2 busy` 不重试、`check_timeouts` 为 best-effort（写冲突跳过） | SDK 手册、白皮书、README 三处一致 |
| T6 | 文档防腐败机制 | 建立"文档-代码一致性检查清单"（版本宏四处同步、API 清单、性能数据、边界表），作为 release checklist | 清单入库；release 时逐项勾选 |

### v3.3 ——「跨进程与工业语义」版本

**边界：能力补齐；只复用 128B 点位内已有字节（含 19B padding），不新增字段、不改既有偏移，ABI 保持 v1。**

| 任务 | 内容 | 约束/验收 |
|---|---|---|
| 跨进程变更通知 | 新增跨进程事件通道（eventfd / Unix socket / shm+futex 三选一，**方案需专项评审**） | 端到端通知延迟实测；soak 验证无 fd/shm 泄漏；TDD 红-绿 |
| 写冲突语义确定化 | 有限次自旋重试 + 明确返回语义 + 文档化 | bench 验证吞吐不退化；多写者成功率有实测 |
| 类型扩展 | int64 / uint32 / float（**复用 value union 内已有字节**）；blob 需评审是否放得下 | ABI 不变；`sizeof(indurtdb_point_t)==128` 断言仍通过 |
| 语义扩展 | SourceTimestamp 注入、`status_code`(2B) 兼容 OPC UA StatusCode、deadband(4B)/RBE | 仅占用 padding 19B 中的 14B，余 5B 预留 |
| 可靠性 | shm 头部 CRC + 版本协商 + 版本不匹配处理策略 | attach 时行为明确定义并有测试 |
| 诊断与工具 | fd/shm 泄漏检测、per-point 统计、Python 巡检/冒烟工具集 | 工具自带工程结构；可接入 CI |
| 质量 | 行覆盖 ≥80%；N 小时 soak；SylixOS 交叉编译与目标板验证（依赖工具链） | 零警告；测试全绿 |
| 术语清理（遗留） | 测试用例名 `ZeroCopyPeek`（`tests/integration/test_c_multi_process.cpp`）与历史术语仍称"零拷贝"，v3.2 仅改注释未改名（受"只改文档"边界约束） | v3.3 随代码变更一并重命名并回归 |

> **重要约束**：`indurtdb_point_t` 现有 padding 仅 **19 字节**。SourceTimestamp(8B) + status_code(2B) + deadband(4B) = 14B 可容纳；**EURange 工程量程（min/max 各 8B）放不下**，须推迟到 v4.0 布局 v2 或另设元数据区，评审时确认。

### v4.0 ——「架构级」版本（需重构评审）

**边界：允许 ABI 变更；必须配套迁移工具与向前兼容方案（对应 SDK 向前兼容约定，避免迫使使用者重编二进制）。**

- **shm 内 name→id 索引**（D3）：全局一致的点位名索引 + 点位发现能力
- 去单例化：同进程多实例
- 容量在线扩容（或分段增长）
- shm 布局 v2 + 版本迁移工具 + 双版本共存验证
- EURange / 工程量程等需要新增字段的语义项

### 版本与 tag

每个版本打包节点在 git 打 tag 并标注版本信息；版本号在 `VERSION`、`CMake project(VERSION)`、`indurtdb.h` 版本宏、`README`/`CHANGELOG` 四处同步。**版本边界一经确定不得随意扩张**；新增需求落入下一版本或另立版本。

---

## 8. 附：与其他文档的关系

- 本文更新 `docs/06-开发规划/05-技术演进规划.md` 的差距清单（G1–G7 已部分关闭，本文新增 G8–G20）与版本边界。
- 定位结论（D1）须回写 `README.md` 与 `docs/01-白皮书/`（白皮书 4.0）。
- 本文不替代各任务实现计划；每个任务落地时**另建分支** + 另建实现计划（遵循 TDD 与评审约定）。
